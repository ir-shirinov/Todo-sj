// vars

$PRIMARY = #ff0000
$GREEN = green
$LIGHT_GREEN = lighten($GREEN, 15%)
$DARK_GREEN = darken($GREEN, 40%)
$BLACK = black
$WHITE = white

// functions

$GRID_BASE = 10px

gutter(size = 1) {
  return size * $GRID_BASE
}
